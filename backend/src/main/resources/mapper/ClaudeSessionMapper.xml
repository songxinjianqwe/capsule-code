<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinjian.capsulecode.mapper.ClaudeSessionMapper">

    <select id="findSessionId" resultType="java.lang.String">
        SELECT session_id FROM claude_sessions WHERE device_id = #{deviceId}
    </select>

    <insert id="upsert">
        INSERT INTO claude_sessions (device_id, session_id, updated_at)
        VALUES (#{deviceId}, #{sessionId}, #{updatedAt})
        ON DUPLICATE KEY UPDATE
            session_id = #{sessionId},
            updated_at = #{updatedAt}
    </insert>

    <delete id="delete">
        DELETE FROM claude_sessions WHERE device_id = #{deviceId}
    </delete>

</mapper>
