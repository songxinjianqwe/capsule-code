<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinjian.capsulecode.mapper.ChatFileMapper">

    <insert id="insertFile" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_file (file_uuid, file_name, mime_type, file_path, device_id)
        VALUES (#{fileUuid}, #{fileName}, #{mimeType}, #{filePath}, #{deviceId})
    </insert>

    <select id="selectById" resultType="com.xinjian.capsulecode.model.ChatFile">
        SELECT id, file_uuid, file_name, mime_type, file_path, device_id
        FROM chat_file
        WHERE file_uuid = #{fileUuid}
    </select>

</mapper>
